security:
  basic:
    enabled: false

management:
  security:
    enabled: false

logging:
  level:
    io:
      pivotal: DEBUG

spring: # <---NEW SECTION
  cloud:
    services:
      registrationMethod: route

ribbon: # <---NEW SECTION
  IsSecure: false

test:
  setting: test-setting-value-from-config-server-app-config-yml-updated2
